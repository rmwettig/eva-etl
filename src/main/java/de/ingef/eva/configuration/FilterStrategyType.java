package de.ingef.eva.configuration;

public enum FilterStrategyType {
	/**
	 * match values against specified regular expression
	 */
	PATTERN,
	/**
	 * match values against set of unqiue values
	 */
	UNIQUE_SET
}
